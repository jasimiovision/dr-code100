export interface keyframeData {
    id: string
    project_id: string
    filename: string
    video_filename: string
    position_coordinates: Array<number>
    fov_coordinates: Array<number>
    image_size: Array<number>
    verified_status: boolean
  }